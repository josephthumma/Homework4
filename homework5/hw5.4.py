def encrypt(write):
    translated = ''
    i = len(write)
    while i >= 0;
    translated = translated + write is :'', translated)
    return translated
    def decrpt(write):
        translated = '
        i = len(write) - 1
        while i>0:
              translated = translated + write[i]
              i=i+1
              print("the decrypt wite ",translated)
              message = "cleae demo"
              x=encrypt(message)
              print(x)
              decrpt(x)